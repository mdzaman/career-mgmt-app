import 'package:flutter/material.dart';

class SpaceH8 extends StatelessWidget {
  final double height;
  const SpaceH8({this.height = 8.0});

  @override
  Widget build(BuildContext context) {
    return SizedBox(height: height);
  }
}

class SpaceH12 extends StatelessWidget {
  final double height;
  const SpaceH12({this.height = 12.0});

  @override
  Widget build(BuildContext context) {
    return SizedBox(height: height);
  }
}
class SpaceH2 extends StatelessWidget {
  final double height;
  const SpaceH2({this.height = 2.0});

  @override
  Widget build(BuildContext context) {
    return SizedBox(height: height);
  }
}
class SpaceH16 extends StatelessWidget {
  final double height;
  const SpaceH16({this.height = 16.0});

  @override
  Widget build(BuildContext context) {
    return SizedBox(height: height);
  }
}

class SpaceH20 extends StatelessWidget {
  final double height;
  const SpaceH20({this.height = 20.0});

  @override
  Widget build(BuildContext context) {
    return SizedBox(height: height);
  }
}

class SpaceH24 extends StatelessWidget {
  final double height;
  const SpaceH24({this.height = 24.0});

  @override
  Widget build(BuildContext context) {
    return SizedBox(height: height);
  }
}

class SpaceW8 extends StatelessWidget {
  final double width;
  const SpaceW8({this.width = 8.0});

  @override
  Widget build(BuildContext context) {
    return SizedBox(width: width);
  }
}

class SpaceW16 extends StatelessWidget {
  final double width;
  const SpaceW16({this.width = 16.0});

  @override
  Widget build(BuildContext context) {
    return SizedBox(width: width);
  }
}

class SpaceW20 extends StatelessWidget {
  final double width;
  const SpaceW20({this.width = 20.0});

  @override
  Widget build(BuildContext context) {
    return SizedBox(width: width);
  }
}
